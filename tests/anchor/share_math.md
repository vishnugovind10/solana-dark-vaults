# Anchor test coverage

The portable host tests in `state.rs` cover exact first-deposit share minting, floor rounding, withdrawal bounds, pause enforcement, pending computation epochs, and allocation-sum validation. Full validator/CPI tests remain gated on the Linux Arcium environment described in `docs/local-setup.md`.

